@inherits TradeContainerBase

@if (!IsCompact)
{
    <FiltersContainer OnSearch="@(s => StateHasChanged())" Query="@(TradeState.CurrentTradeSession?.Query)"></FiltersContainer>
}

<ItemsContainer TradeSession="@TradeState.CurrentTradeSession" IsCompact="@IsCompact"></ItemsContainer>