@using System.Text.Json
@inherits TradeContainerBase
<FiltersContainer OnSearch="@(s => SearchResult = s)"></FiltersContainer>
<ItemsContainer SearchResult="@SearchResult"></ItemsContainer>

@JsonSerializer.Serialize(SearchResult)