@using PoECommerce.Core.Model.Trade
@inherits ItemsContainerBase

<h3>ItemsContainer</h3>

@if (SearchResult != null)
{
    @if (ListedItems == null || ListedItems.Count == 0)
    {
        @:No results found
    }
    else
    {
        @:Showing @SearchResult.ItemIds.Length results (@SearchResult.Total matched)
        foreach (ListedItem resultItem in ListedItems)
        {
            <ListedItemContainer ListedItem="@resultItem" PoEWindow="@PoEWindow"></ListedItemContainer>
        }
    }
}